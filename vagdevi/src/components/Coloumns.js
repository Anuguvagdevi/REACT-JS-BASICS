import React from 'react'

function Coloumns() {
    return (
        <React.Fragment>
            {/* {
                items.map(item =>(
                    <React.Fragment key={item.id} >
                    <h1>Title</h1>
                    <p>{item.Title}</p>
                     </React.Fragment>
                ))
            } */}
            <td>Name</td>
            <td>Vagdevi</td>
        </React.Fragment>
    )
}

export default Coloumns
